package app;

import javafx.application.Application;

/**
 * Future entry point for the SITM-MIO concurrent version.
 */
public class ConcurrentMain {
    public static void main(String[] args) {
        Application.launch(ConcurrentSitmMioApplication.class, args);
    }
}
